# Infrastructure Notes

The local Bicep file is a legacy reference. **Do not use it as the lab deployment path.**
Deploy the Microsoft Grubify starter lab (see [resources README](../README.md)), or use
the fallback packet.
