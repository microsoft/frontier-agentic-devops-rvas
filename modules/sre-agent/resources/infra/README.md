# Infrastructure Notes

The Azure SRE Agent track uses the official Microsoft starter lab for live infrastructure:

```text
https://github.com/microsoft/sre-agent/tree/main/labs/starter-lab
```

The local Bicep file remains as a legacy reference. **Do not use it as the primary lab deployment path.** Deploy the Microsoft Grubify starter lab for live delivery, or use the fallback packet.
