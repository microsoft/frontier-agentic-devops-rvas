# Infrastructure Notes

The rebuilt Azure SRE Agent track uses the official Microsoft starter lab for live infrastructure:

```text
https://github.com/microsoft/sre-agent/tree/main/labs/starter-lab
```

The local Bicep file in this folder is retained only as a legacy coach reference. Do not use it as the primary lab deployment path. For live delivery, deploy the Microsoft Grubify starter lab or use a coach-provided fallback evidence packet.
