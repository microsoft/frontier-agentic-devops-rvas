id: ghaw-11
title: The Relay
module: ghaw
track: continuous-intelligence
difficulty: advanced
duration_minutes: 30
prerequisites:
  - ghaw-00
prerequisite_capabilities:
  - GitHub Actions basics
  - YAML syntax
  - gh-aw CLI installed — see ghaw-00
description: Unlock workflow chaining — build a producer that writes structured data to repo-memory and a consumer that reads it, demonstrating how independent agents pass state to each other.
success_criteria:
  - Daily trigger works (`on: schedule:`)
  - JSON file written to `repo-memory/metrics/{date}.json` with correct structure
  - Workflow runs without errors (check Actions logs)
  - `safe-outputs: noop` is called
  - Weekly trigger works
  - Reads from `repo-memory` using `file-glob: metrics/**/*.json`
  - Discussion created with trend analysis
  - Workflow correctly interprets the JSON from the producer
  - Producer runs, data appears in `repo-memory/`
  - Consumer reads that data and creates a discussion referencing the metrics
  - The two workflows are not directly coupled (consumer doesn't know producer's name)
take_home_action: In a repository you own, split one oversized automation into producer and consumer workflows that exchange repo-memory state and write only through declared safe-outputs.
take_home_owner: solution-architect
take_home_signal: The consumer run uses producer data from the real repo and emits the expected safe output without manual handoff.
tags:
  - workflow-chaining
  - repo-memory
  - producer-consumer
  - state-persistence
app_dependency: none
emu_compatible: true
min_environment: repo
tier: core
source_repo: microsoft/frontier-agenticdevops-hackathon
source_path: modules/ghaw/challenges/11-the-relay/meta.yml
license: MIT
references:
  - https://github.github.com/gh-aw/reference/repo-memory/
  - https://github.com/github/gh-aw/blob/main/.github/workflows/metrics-collector.md
  - https://github.com/github/gh-aw/blob/main/.github/workflows/agent-performance-analyzer.md
  - https://github.github.com/gh-aw/reference/safe-outputs/
  - https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#onschedule
